SELECT LENGTH(name::varchar) AS id, LENGTH(legs::varchar) AS name, LENGTH(arms::varchar) AS legs,
       LENGTH(characteristics::varchar) AS arms, LENGTH(id::varchar) AS characteristics
FROM monsters;
